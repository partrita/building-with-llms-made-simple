# Marimo 노트북 작성 규칙

## 파일 구조

모든 Marimo 노트북은 파이썬 버전과 의존성을 지정하는 스크립트 헤더 블록으로 시작해야 합니다:

```python
# /// script
# requires-python = ">=3.12"
# dependencies = [
#     "llamabot[all]==version",
#     "package1==version",
#     "package2==version",
# ]
# ///
```

헤더 다음에는 marimo를 임포트하고 앱 너비를 설정합니다:

```python
import marimo

__generated_with = "0.13.11"  # 필요에 따라 버전 업데이트
app = marimo.App(
    width="medium",  # 또는 더 넓은 노트북의 경우 "full"
)
```

## 셀 구성

첫 번째 셀은 marimo를 임포트하고 mo 객체를 반환해야 합니다:

```python
@app.cell
def _():
    import marimo as mo
    return (mo,)
```

**중요**: 변수 이름은 Marimo 노트북의 모든 셀에서 고유해야 합니다. 다른 셀 간에 변수 이름(루프 변수 포함)을 재사용하지 마십시오. 충돌이 발생할 수 있습니다. 각 변수 이름은 전체 노트북에서 한 번만 정의할 수 있습니다.

마크다운 셀(mo.md)은 노트북에서 여러 용도로 사용됩니다:

* 주 제목 (h1)
* 섹션 헤더 (h2)
* 하위 섹션 헤더 (h3)
* 설명 텍스트
* 연습 문제 지침
* 수업 토론
* 요약

마크다운 셀에는 항상 `hide_code=True`를 사용하십시오:

```python
@app.cell(hide_code=True)
def _(mo):
    mo.md(
        r"""
        ## 섹션 제목

        내용은 여기에 들어갑니다.
        """
    )
    return
```

코드 셀은 구현 세부 사항에 중점을 두어야 합니다:

* 필요한 패키지 임포트
* 클래스 및 함수 정의
* 봇 인스턴스 생성
* 봇 호출 실행
* 다른 셀에서 사용할 관련 변수 반환

## 콘텐츠 구성

명확한 제목과 소개로 노트북을 구성하십시오. h2 헤더를 사용하여 콘텐츠를 논리적 섹션으로 나누고 특정 주제에는 h3 헤더를 사용하십시오. 새로운 개념을 소개한 후 연습 문제를 포함하고 요약 섹션으로 마무리하십시오.

## 코드 스타일

다음과 같이 높은 코드 품질을 유지하십시오:

* 함수 매개변수 및 반환 값에 타입 힌트 사용
* 클래스 및 함수에 독스트링으로 문서화
* 설명적인 변수 이름 사용
* 셀 출력을 집중적이고 관련성 있게 유지
* 후속 셀에서 사용될 변수 반환

## 봇 구현

봇을 구현할 때:

* 시스템 프롬프트를 명확하고 간결하게 정의
* 템플릿 프롬프트에 @lmb.prompt 데코레이터 사용
* 적절한 온도 값 설정 (결정론적의 경우 0.0, 창의적인 경우 더 높게)
* 모델 선택과 그 의미를 문서화

## 모범 사례

노트북을 유지보수 가능하고 효과적으로 유지하려면:

* 각 셀을 단일 개념에 집중
* 설명을 위해 마크다운을, 구현을 위해 코드를 사용
* 학습을 강화하기 위해 연습 문제 포함
* 가정과 요구 사항을 문서화
* 일관된 서식 유지
* 적절한 오류 처리 구현
* 복잡한 로직에 주석 추가

## 일반적인 패턴

### 봇 생성

```python
bot = lmb.SimpleBot(
    system_prompt="명확하고 구체적인 프롬프트",
    model_name="모델_이름",
    temperature=0.0,  # 필요에 따라 조정
)
```

### 마크다운 섹션

```python
mo.md(
    r"""
    ## 섹션 제목

    내용은 여기에 들어갑니다.
    """
)
```

### 연습 문제

```python
mo.md(
    r"""
    ### 연습 문제

    여기에 연습 문제 지침을 입력하십시오.
    """
)
```

## 파일 이름 지정

노트북의 목적을 명확하게 나타내는 설명적인 이름을 선택하십시오. 순서가 있는 노트북의 경우 숫자 접두사(예: 01_, 02_)를 사용하십시오. 공백을 밑줄로 바꾸고 항상 .py 확장자를 사용하십시오.

## 의존성

의존성을 효과적으로 관리하려면:

* 스크립트 헤더에 필요한 모든 패키지 나열
* 재현성을 위해 정확한 버전 지정
* 직접 및 간접 의존성 모두 포함
* 필요에 따라 버전 업데이트
* 모든 구성 요소를 사용할 수 있도록 `llamabot` 대신 `llamabot[all]` 사용
* 개별 llamabot 구성 요소의 별도 설치 방지

## 문서화

명확하고 포괄적인 문서를 제공하려면:

* 개념에 대한 명확한 설명 포함
* 주석으로 코드 문서화
* 관련 예제 제공
* 적절한 경우 외부 리소스에 대한 참조 포함

## 노트북

Marimo 노트북 산문을 편집할 때, 핵심 사항을 강조하기 위해 글머리 기호를 전략적으로 사용하면서 간결한 작성을 우선시하십시오. 일관된 서식을 위해 항상 MarkdownLint 규칙을 준수하십시오.

## MarkdownLint 규칙

일관된 서식을 위해 모든 MarkdownLint 규칙을 따르십시오.
노트북은 `mo.md()` 코드 셀 문자열 내에서도 MarkdownLint 규칙을 준수해야 합니다.

## 참조 문서 스타일

참조 문서(예: references.md)를 작성할 때:

* H1 및 H2 헤더로 명확한 계층 구조 사용
* 대시(-)를 사용하여 글머리 기호 서식을 일관되게 지정
* 링크를 `[제목](URL) - 설명`으로 구성
* 각 링크 뒤에 간결하고 설명적인 요약 제공
* 명확한 섹션 헤더 아래에 관련 리소스 그룹화
* 섹션 간 일관된 간격 유지
* 콘텐츠 범주를 명확하게 나타내는 설명적인 섹션 제목 사용
* 각 리소스의 주요 측면을 강조하여 설명을 집중적이고 유익하게 유지
* 주제 또는 테마별로 콘텐츠를 논리적으로 구성
* 모든 링크가 유효하고 올바르게 서식되었는지 확인
